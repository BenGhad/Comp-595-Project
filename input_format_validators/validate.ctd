INT(1,200000,n) SPACE INT(2,2147483647,m) NEWLINE
SET(ks = 0)
REP(n,NEWLINE)
    INT(2,200000,k) NEWLINE
    SET(ks = ks + k)
    REP(k,SPACE)
        INT(-2147483648,2147483647)
    END
END

ASSERT(2 <= ks && ks <= 200000)
NEWLINE
